#include "particle_filter.h"

struct particle_filter_impl : public ParticleFilter {
    TrackedState run(const Frame& frame) override {
        // Implement particle filter algorithm
        TrackedState state;
        return state;
    }
};
